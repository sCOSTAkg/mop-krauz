# 🚀 Настройка Real-Time Синхронизации через Supabase

## Обзор

Система синхронизации **KRAUZZZZ** теперь поддерживает **real-time синхронизацию** через Supabase в дополнение к Airtable. Это обеспечивает:

✅ **Мгновенные обновления** - изменения применяются немедленно без задержки
✅ **Двусторонняя синхронизация** - изменения в Airtable автоматически отражаются в Supabase и наоборот
✅ **Fallback режим** - если real-time недоступен, используется polling каждые 2 минуты
✅ **Offline-first** - данные сохраняются локально и синхронизируются при подключении

---

## Шаг 1: Создание проекта в Supabase

1. Зайдите на [supabase.com](https://supabase.com)
2. Нажмите **"New Project"**
3. Заполните данные:
   - **Name**: `krauzzzz` (или любое название)
   - **Database Password**: создайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион (например, `eu-central-1`)
4. Нажмите **"Create new project"**
5. Подождите 2-3 минуты, пока проект создается

---

## Шаг 2: Настройка базы данных

1. В левом меню выберите **Database → SQL Editor**
2. Нажмите **"New Query"**
3. Откройте файл `supabase-setup.sql` из корня проекта
4. Скопируйте весь SQL код и вставьте в редактор
5. Нажмите **"RUN"** (или `Ctrl+Enter`)
6. Подождите выполнения скрипта (~10-15 секунд)

✅ **Готово!** Все таблицы созданы и настроены для real-time синхронизации.

---

## Шаг 3: Получение API ключей

1. В левом меню выберите **Settings → API**
2. Скопируйте следующие значения:

   ```
   Project URL: https://xxxxxxxxxxxxx.supabase.co
   anon public key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

---

## Шаг 4: Обновление .env.local

1. Откройте файл `.env.local` в корне проекта
2. Найдите секцию **SUPABASE CONFIGURATION**
3. Замените placeholder значения на реальные:

   ```env
   # SUPABASE CONFIGURATION
   VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.XXXXXXX...

   # Включить real-time синхронизацию
   VITE_ENABLE_REALTIME_SYNC=true
   ```

4. Сохраните файл

---

## Шаг 5: Перезапуск приложения

```bash
# Остановите dev server (Ctrl+C)
# Запустите заново
npm run dev
```

---

## Проверка работы

После запуска приложения:

1. Откройте консоль браузера (F12)
2. Проверьте логи:
   ```
   ✅ Supabase real-time initialized
   🚀 Setting up real-time sync...
   ✅ Subscribed to modules changes
   ✅ Subscribed to materials changes
   ✅ Real-time subscriptions active
   ✅ Real-time mode active - polling disabled
   ```

3. Попробуйте изменить данные в Airtable:
   - Откройте Airtable
   - Измените название модуля
   - В течение 1-2 секунд изменение должно отобразиться в приложении

4. Проверьте логи real-time обновлений:
   ```
   📡 Real-time UPDATE on modules
   📄 Fetching all content from Airtable...
   ✅ Content loaded
   ```

---

## Архитектура синхронизации

```
┌─────────────────┐         ┌──────────────────┐
│   ПРИЛОЖЕНИЕ    │◄────────┤  SUPABASE        │
│   (Frontend)    │   RT    │  (Real-time DB)  │
└────────┬────────┘  sync   └──────────────────┘
         │
         │ Polling
         │ (fallback)
         ▼
┌─────────────────┐         ┌──────────────────┐
│   AIRTABLE      │◄────────┤  ADMIN PANEL     │
│   (Master DB)   │  Write  │  (Управление)    │
└─────────────────┘         └──────────────────┘
```

### Как это работает:

1. **Airtable** - основная база данных (источник истины)
2. **Supabase** - real-time слой для мгновенных обновлений
3. **Приложение** подписывается на изменения в Supabase
4. **Webhook/Polling** синхронизирует Airtable → Supabase
5. **Изменения в приложении** сохраняются в оба источника

---

## Режимы работы

### 1️⃣ Real-time режим (по умолчанию)
- Supabase настроен и доступен
- Мгновенные обновления через WebSocket
- Polling отключен
- Логи: `✅ Real-time mode active`

### 2️⃣ Polling режим (fallback)
- Supabase не настроен или недоступен
- Обновления каждые 2 минуты
- Работает только с Airtable
- Логи: `📊 Starting polling mode (interval: 2 min)`

---

## Настройка Row Level Security (RLS)

По умолчанию все таблицы доступны для чтения и записи. Для повышения безопасности:

### Ограничить запись только для админов

```sql
-- В Supabase SQL Editor выполните:
DROP POLICY IF EXISTS "Enable insert/update for modules" ON public.modules;

CREATE POLICY "Enable insert/update for admins only" ON public.modules
  FOR ALL
  USING (auth.jwt() ->> 'role' = 'admin');
```

### Ограничить доступ к личным данным

```sql
DROP POLICY IF EXISTS "Enable read access for all users" ON public.users;

CREATE POLICY "Users can only see their own data" ON public.users
  FOR SELECT
  USING (telegram_id = auth.jwt() ->> 'sub');
```

---

## Мониторинг и отладка

### Проверка подключения

```typescript
// В консоли браузера:
import { supabaseService } from './services/supabaseService';

// Проверить статус
console.log(supabaseService.isReady()); // true / false

// Health check
await supabaseService.healthCheck(); // true / false
```

### Логи в консоли

```javascript
// Включить подробные логи
localStorage.setItem('debug', 'true');

// Отключить
localStorage.removeItem('debug');
```

### Supabase Dashboard

1. Зайдите в **Database → Tables**
2. Выберите таблицу (например, `users`)
3. Проверьте данные в реальном времени

---

## Миграция данных из Airtable в Supabase

Если вы хотите перенести существующие данные:

```bash
# TODO: Создать миграционный скрипт
npm run migrate:airtable-to-supabase
```

(Скрипт в разработке)

---

## Troubleshooting

### ❌ "Supabase not configured"
- Проверьте `.env.local` - credentials должны быть без `XXXX`
- Убедитесь, что `VITE_ENABLE_REALTIME_SYNC=true`
- Перезапустите dev server

### ❌ "Failed to subscribe"
- Проверьте, что SQL скрипт выполнен
- Убедитесь, что `ALTER PUBLICATION` команды выполнены успешно
- Проверьте Supabase Dashboard → Database → Replication

### ❌ "CHANNEL_ERROR"
- Проверьте названия таблиц (должны совпадать с SQL скриптом)
- Проверьте RLS политики (должны разрешать SELECT)
- Проверьте логи в Supabase Dashboard → Logs

### ❌ Данные не обновляются
- Откройте консоль и проверьте логи
- Проверьте Network → WS (должны быть WebSocket соединения)
- Убедитесь, что данные изменяются в Supabase, а не только в Airtable

---

## FAQ

### Q: Нужно ли платить за Supabase?
**A:** Нет, free tier включает:
- 500 MB database
- 1 GB file storage
- 2 GB bandwidth
- 50,000 MAU (monthly active users)

Этого достаточно для большинства проектов.

### Q: Можно ли использовать только Airtable без Supabase?
**A:** Да, система автоматически переключится на polling режим (обновления каждые 2 минуты).

### Q: Можно ли использовать только Supabase без Airtable?
**A:** Да, но нужно будет адаптировать код для работы напрямую с Supabase.

### Q: Как синхронизировать Airtable → Supabase автоматически?
**A:** Можно настроить Airtable Automation или webhook, который будет обновлять Supabase при изменениях.

---

## Дополнительные ресурсы

- [Supabase Documentation](https://supabase.com/docs)
- [Realtime Guide](https://supabase.com/docs/guides/realtime)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)
- [Airtable API](https://airtable.com/developers/web/api/introduction)

---

**Готово!** Теперь у вас настроена полноценная real-time синхронизация. 🎉

При возникновении проблем создайте issue на GitHub или напишите в поддержку.
